import styled from 'styled-components';

export const formItemStyle = styled.div`
     border: none;
     display: flex;
     flex-direction: column;
     padding: 0;
     width: 100%;
     margin: 0;
     position: relative;
`;